(function () {
	'use strict';

	angular.module('rapp')
		.controller('ProfileCtrl', ['DataCtx', 'localStorageService', 'ngDialog', '$rootScope', '_m', '$scope', ProfileCtrl]);

	function ProfileCtrl(DataCtx, localStorageService, ngDialog, $rootScope, _m, $scope) {
		var vm = this;

	}
})();
